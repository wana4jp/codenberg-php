<?php
/**
  vim: ft=php
*/

$finder = PhpCsFixer\Finder::create()->in(['lib/', 'test/']);

return PhpCsFixer\Config::create()
    ->setRiskyAllowed(true)
    ->setRules([
        '@PSR2' => true,
    ])
    ->setFinder($finder);
